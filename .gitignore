.cache
.cloud
.ha_run.lock
.storage
*.bak
*.db*
*.log*
**/*.swp
**/__pycache__
**/trash
Abode
custom_components
deps
esphome/archive
image
**/secrets.yaml
tts
zigbee2mqtt/coordinator_backup.json
zigbee2mqtt/state.json
zigbee2mqtt/configuration_backup*.yaml

# Private deploy key used only by scripts/commit_ha_version.sh (see
# packages/config_repo_sync.yaml) to push .HA_VERSION bumps. Scoped to
# this repo only via a GitHub Deploy Key -- never commit the private key.
.ssh/
